# Example environment variables for the Express.js RESTful API
# Copy this file to `.env` and set values as needed.

# The port the server will listen on (optional). Default is 3000 if unset.
PORT=3000

# API key required for write operations (POST/PUT/DELETE) via the `x-api-key` header.
# Change this to a strong secret in production.
API_KEY=dev-secret-key
